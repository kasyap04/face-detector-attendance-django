
class AppError:
    IMG_NOT_FOUND = (100, "Image not found")
    FACE_NOT_FOUND = (101, "Face not found")
    API_PAYLOAD_NOT_FOUND = (102, "API payload not found")
    STUDENT_ALREADY_REGISTERD = (103, "Student already registered")
    STUDENT_ALREADY_PRESENT = (104, "Student already present")
